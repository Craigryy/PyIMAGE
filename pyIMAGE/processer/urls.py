# from django.urls import path,include
# from allauth.account.views import LoginView

# # from .views import Home, ImageProcessing, DeleteImage, SaveProcessedImage, Effects


# app_name = 'main'

# urlpatterns = [
#     # path('', Home.as_view(), name='home'),
#     # path('effects/', Effects.as_view(), name='effects'),
#     # path('process_image/', ImageProcessing.as_view(), name='process_image'),
#     # path('save_processed_image/', SaveProcessedImage.as_view(), name='save_processed_image'),
#     # path('delete_image/<int:id>/', DeleteImage.as_view(), name='delete_image'),
  
# ]
# facebook_login/urls.py

from django.urls import path
from .views import view_name

urlpatterns = [
   
]